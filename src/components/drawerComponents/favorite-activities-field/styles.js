import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  icon: {
    color: "#ff9800",
  },
  favoriteActivities: {
    marginTop: "0.6em",
  },
});
export default useStyles;
